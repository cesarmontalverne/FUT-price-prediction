---
title: "FUT-price-prediction"
author: "cesar and pete"
date: "2023-01-02"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
```

## R Markdowns
```{r}
players <- read.csv("fut_bin21_players.csv")
keepers <- players[players$position=="GK",]
line <- players %>% filter(position!="GK")
```

```{r}
#table(keepers$league)
#table(keepers$quality)
#table(keepers$quality)
table(keepers$origin)
nrow(keepers)
```
```{r}
str(keepers)
```
```{r}
keepers <- keepers[keepers$league != "Icons",]
na_rows <- c("origin", "pace", "dribbling", "passing","defending", "shooting", "physicality")
keepers <- keepers[,!names(keepers)%in%na_rows]

```